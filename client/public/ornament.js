// ornament.js
document.addEventListener("DOMContentLoaded", () => {
    if (typeof loadProducts === 'function') {
        // Match exactly how you typed the category in the admin panel!
        loadProducts('ornament'); 
    }
});